# Hides inherited framework members (ASP.NET Core, System.*) from the generated
# pages, so each type only documents the members defined in lumify.api itself.
apiRules:
  - exclude:
      uidRegex: ^Microsoft(\.|$)
      type: Namespace
  - exclude:
      uidRegex: ^System(\.|$)
      type: Namespace
